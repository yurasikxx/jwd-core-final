package epam.domain;

/**
 * Expected fields:
 * sex {@link Sex} - required
 * role {@link Role} - member role - required
 * rank {@link Rank} - member rank - required
 * missions {@link FlightMission} - list of all available missions for this member - NOT required
 */
public class CrewMember extends AbstractBaseEntity {
    // todo
}
